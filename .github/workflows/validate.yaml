name: Pull Request validation

on: [pull_request]


jobs:
  ASPA-contracts-ci:
    name: Run ASPA-contracts validation
    uses: ./.github/workflows/ASPA-contracts-ci.yaml

  ASPA-frontend-ci:
    name: Run ASPA-frontend validation
    uses: ./.github/workflows/ASPA-frontend-ci.yaml

